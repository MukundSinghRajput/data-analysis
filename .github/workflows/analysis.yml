name: Run Python Script

on:
  push:
    branches: [MukunD]
  pull_request:
    branches: [MukunD]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run script
        run: python main.py

      - name: List generated files
        run: |
          ls -R analysis || echo "No analysis folder generated"

      - name: Commit changes (optional)
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # Only add specific files if needed, e.g., Hypermart_cleaned.csv
          git add analysis/Hypermart_cleaned.csv || echo "No cleaned CSV to commit"
          git commit -m "Auto update cleaned CSV from GitHub Actions" || echo "No changes to commit"

      - name: Push changes (optional)
        uses: ad-m/github-push-action@master
        if: success()
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: MukunD
          force: true
